use std::process::Command;

fn main() {
    // build_sass_files();
}

// fn build_sass_files() {
//     _ = Command::new("sass")
//         .arg("assets/:build/assets/")
//         .status()
//         .expect("failed to execute sass");
// }
